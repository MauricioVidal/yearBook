<%= form_for(@visitum) do |f| %>
  <% if @visitum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@visitum.errors.count, "error") %> prohibited this visitum from being saved:</h2>

      <ul>
      <% @visitum.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current_user_id %><br>
    <%= f.number_field :current_user_id %>
  </div>
  <div class="field">
    <%= f.label :user_visited_id %><br>
    <%= f.number_field :user_visited_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
